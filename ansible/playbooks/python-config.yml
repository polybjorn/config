# ${XDG_CONFIG_HOME}/ansible/playbooks/python-config.yml

- name: Export Python configuration files from repository
  hosts: localhost
  connection: local

  vars:
    config_home: ${XDG_CONFIG_HOME}
    config_repo: ${REPOSITORIES}/config

  tasks:
    - name: Export python scripts
      ansible.builtin.copy:
        src: "{{ config_repo }}/python/"
        dest: "{{ config_home }}/python/"
        mode: '0644'